<!doctype html>
<html lang="{{ lang | default('en-US') }}">
  {% include "head.njk" %}
  <body>
    <div class="col-lg-8 mx-auto p-3 py-md-5">
      {% include "header.njk" %}
      <main>
        {{ content | safe }}
      </main>
      {% include "footer.njk" %}
    </div>
  </body>
</html>
